#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

typedef struct Node {
    int data;
    struct Node *next;
} Node;

void append(Node **head) {
}

Node** find(Node** head, Node* target) {

}

int main(void) {

    return 0;
}
